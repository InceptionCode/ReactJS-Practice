import React from 'react';
import ReactDOM from 'react-dom';
import {Router, Route, IndexRoute, hashHistory} from "react-router";
import Heading from './js/components/Heading.jsx';


ReactDOM.render(
  <Heading />,
  document.getElementById('build')
);
